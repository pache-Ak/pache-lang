#include "compunit.h"

/* llvm::AllocaInst *
pache::compunit_ast::find_named_Value(std::string const &name) const {
  auto it = m_named_values.find(name);
  if (it != m_named_values.end()) {
    return it->second;
  } else if (m_father != nullptr) {
    return m_father->find_named_Value(name);
  } else {
    // TODO log error
    return nullptr;
  }
}; */

/* void pache::compunit_ast::insert_named_Value(std::string const &name,
                                             llvm::AllocaInst *val) {
  m_named_values[name] = val;
} */